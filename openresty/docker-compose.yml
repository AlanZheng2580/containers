version: '3.3'

services:
  openresty:
    image: cyching/openresty:latest
    ports:
      - '8088:8088'
    volumes:
      - ./nginx.conf:/opt/bitnami/openresty/nginx/conf/nginx.conf
  httpbin:
    image: kennethreitz/httpbin
    ports:
      - '80:80'